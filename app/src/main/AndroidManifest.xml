<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.cs506_spike">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CS506_Spike">
        <activity android:name=".CreateAccount"></activity>
        <activity android:name=".CustomerView" />
        <activity android:name=".Orders" android:label="Orders" android:parentActivityName=".RestaurantStaff"/>
        <activity android:name=".RestaurantStaff" />
        <activity android:name=".Admin" />
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>